git init 
git remote add origin "repo url"
git remote -v
git checkout main	

git status
git add .
git commit -m "commit message"
git push origin main
git log
git branch -d branch_name

for merging>>
git checkout your_branch_name
git merge branch_to_merge


git push -f origin main >> forcefully pushed

git checkout "branch"
git rm -r cached
